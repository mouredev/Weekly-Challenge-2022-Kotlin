/*
 * Reto #12
 * ¿ES UN PALÍNDROMO?
 * Fecha publicación enunciado: 21/03/22
 * Fecha publicación resolución: 28/03/22
 * Dificultad: MEDIA
 *
 * Enunciado: Escribe una función que reciba un texto y retorne verdadero o falso (Boolean) según sean o no palíndromos.
 * Un Palíndromo es una palabra o expresión que es igual si se lee de izquierda a derecha que de derecha a izquierda.
 * NO se tienen en cuenta los espacios, signos de puntuación y tildes.
 * Ejemplo: Ana lleva al oso la avellana.
 *
 * Información adicional:
 * - Usa el canal de nuestro discord (https://mouredev.com/discord) "🔁reto-semanal" para preguntas, dudas o prestar ayuda a la comunidad.
 * - Puedes hacer un Fork del repo y una Pull Request al repo original para que veamos tu solución aportada.
 * - Revisaré el ejercicio en directo desde Twitch el lunes siguiente al de su publicación.
 * - Subiré una posible solución al ejercicio el lunes siguiente al de su publicación.
 *
 */

val specialChars = arrayOf(Pair("á", "a"), Pair("é", "e"), Pair("í", "i"), Pair("ó", "o"), Pair("ú", "u"))

fun main(){
    val texto1 = "Reconocer"
    val texto2 = "Golazo"
    val texto3 = "Ana lleva al oso la avellana"
    val texto4 = "El mando de la play esta roto"
    val texto5 = "Amó lA Paloma?"
    val texto6 = "NO PUEDE SEEEEEEEEEEEEEEEEEEEEEEER!!!!!!!"

    println("texto1 en metodo 1: ${isPalindrome1(texto1)}")
    println("texto2 en metodo 1: ${isPalindrome1(texto2)}")
    println("texto3 en metodo 1: ${isPalindrome1(texto3)}")
    println("texto4 en metodo 1: ${isPalindrome1(texto4)}")
    println("texto5 en metodo 1: ${isPalindrome1(texto5)}")
    println("texto6 en metodo 1: ${isPalindrome1(texto6)}")

    println()

    println("texto1 en metodo 2: ${isPalindrome2(texto1)}")
    println("texto2 en metodo 2: ${isPalindrome2(texto2)}")
    println("texto3 en metodo 2: ${isPalindrome2(texto3)}")
    println("texto4 en metodo 2: ${isPalindrome2(texto4)}")
    println("texto5 en metodo 2: ${isPalindrome2(texto5)}")
    println("texto6 en metodo 2: ${isPalindrome2(texto6)}")
}

//Funcion de orden superior
private fun isPalindrome1(text:String):Boolean{
    val simplifiedText = text.lowercase().replace(" ", "").noSpecialChars().noSymbols()
    return simplifiedText == simplifiedText.reversed()
}

//Funcion hecha a mano
private fun isPalindrome2(text: String):Boolean{
    val simplifiedText = text.lowercase().replace(" ", "").noSpecialChars().noSymbols()
    var reversedText = ""
    for (i in simplifiedText.lastIndex downTo 0) reversedText += simplifiedText[i]
    return text == reversedText
}

private fun String.noSpecialChars():String{
    var newStr = this
    for (i in specialChars.indices){
        newStr = newStr.replace(specialChars[i].first, specialChars[i].second)
    }
    return newStr
}

private fun String.noSymbols():String{
    val regex = Regex("[^a-z]")
    return this.replace(regex, "")
}
